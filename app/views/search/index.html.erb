<%= render partial: 'shared/section_heading', locals: {section_title: "Search: " + @term.to_s } %>

<% if @pages.any? %>
  <% @pages.each do |page| %>
    <%= render 'shared/summary', page: page %>
  <% end %>
<% else %>
  <%= render 'shared/result_not_found' %>
<%end  %>
