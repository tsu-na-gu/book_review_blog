
<%= render partial: 'shared/section_heading', locals: {section_title: "Tagged: " + @tag.name } %>

<% @pages.each do |page| %>
  <%= render partial: 'shared/summary', locals: {page: page} %>
<% end %>